{
  "name": "glitchit",
  "version": "1.0.0",
  "description": "GlitchIt social marketplace website",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "preview": "node server.js",
    "build": "node build.js",
    "test": "node --check src/main.js",
    "apk:sync": "node build.js && npx cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  }
}
